type Keys = "xs" | "sm" | "md" | "lg" | "xl";
// type Value = 4 | 6 | 8 | 12 | 16;
// const spacing: Record<Keys, Value> = {

const spacing: Record<Keys, number> = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
};

export default spacing;
